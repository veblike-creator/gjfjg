FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bot_ready_with_genapi.py .
COPY bot_data.db* .  # Если БД уже есть

EXPOSE 8080  # Не обязателен для Telegram bot

CMD ["python", "bot_ready_with_genapi.py"]
